
- navbar

- content_for :title_btn do
  = link_to "Logout", logout_path, rel: :external, data: {role: :button, icon: :back, iconpos: :notext}, method: :delete

%form(action="/mblz/now")
  %label(for='search_q')
  %input(type='search' name='q' id='search_q' placeholder='Search' value="#{params[:q]}")

- list do
  %li(data-icon='plus' data-theme='a')
    = link_to mblz_contacts_path do
      Contacts
      %p.ui-li-aside Last updated first
    = link_to "New", new_mblz_contact_path, "data-theme" => 'g'
  = render 'mblz/contacts/contacts'

