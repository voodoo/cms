# Twilio configuration commented out - Twilio gem not installed and Rails.application.secrets is deprecated
# Keeping only Rails 8, Stimulus, Turbo, and SQLite as per project rules

# Twilio::Config.setup(:account_sid  => Rails.application.credentials.dig(:twilio, :sid),:auth_token   => Rails.application.credentials.dig(:twilio, :token))